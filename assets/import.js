/**
 * Module scripts
 *
 * @see http://stackoverflow.com/questions/17063000/ng-model-for-input-type-file
 */
(function($, module) {

//    'use strict';
/*
    angular.module('cockpit.fields').run(['Contentfields', function(Contentfields) {

        Contentfields.register('fileread', {
            label: 'fileread',
            template: function(model, options) {

                var acceptAttr = (options.accept) ?
                    'accept="' + options.accept + '" ' :
                    ' ';

                return '<input type="file" ' + acceptAttr + 'ng-model="' + model + '" />';
            }
        });
    }]);
*/
})(window.jQuery, window.App.module);
